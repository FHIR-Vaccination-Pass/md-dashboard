import AuthLayout from "../../theme/layouts/Auth";

var routes = [
    {
        path: "/auth",
        name: "Auth",
        component: AuthLayout,
        layout: "/auth",
    }
];
export default routes;